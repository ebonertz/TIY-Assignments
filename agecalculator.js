<doctype! JavaScript>

    function calculateAge(yourAge) {
    var currentYear = Date.now() - birthday.getTime();
    var birthYear = new Date(ageDifMs);


    return (You are either yourAge or yourAge - currentYear)   ;
}
