<doctype! JavaScript>
function verbing (value) {
    if (value.length < 3) return word;
    if (value.slice(-3) === 'ing') {
     return value + ly;
 }  else {
     return value + 'ing';
     }
}

console.log(verbing("skank"))
